# generated files
target/
generated/

# dependencies
Dependencies.toml

# config files
Config.toml
# the config files used for testing, Uncomment the following line if you want to commit the test config files
#!**/tests/Config.toml

# backup of former submodule .git directories (preserved for history access)
_submodule_history_backup/

# large SNOMED CT release archive (>100MB, exceeds GitHub limit)
SnomedCT_InternationalRF2_PRODUCTION_20260501T120000Z.zip
SnomedCT_*.zip
