# Upstream

Imported from `git@github.com:a-rastin/Suicide-Risk.git` tag `v1.0.0` at `d5f462f2cabea14b042da056bdd5bbe9eb5ee1c8`.

`app.js`, `index.html`, `styles.css`, `README.md`, and `scoring-check.js` originate from that revision. Insight integration adds `server.js`, workflow activation, isolated persistence, auth/CSRF, and same-origin routing. The source scoring questions and thresholds are unchanged.
